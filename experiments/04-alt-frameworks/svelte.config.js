// Svelte 5. Runes are the point of including it — the doc calls fine-grained reactivity the
// sharpest test, and `$state`/`$derived` are how Svelte 5 does it.
export default { compilerOptions: { runes: true } }
