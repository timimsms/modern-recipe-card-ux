dist/
node_modules/
sources/
docs/design/
pnpm-lock.yaml

# Generated by scripts/build-tokens.mjs. A test asserts it matches the TypeScript source
# byte for byte, so reformatting it here would break the build.
packages/tokens/tokens.css

# Prose is hand-wrapped and hand-aligned. Reformatting it produces large diffs of pure noise
# on top of real changes, which is exactly what makes a review hard to read.
*.md

_site/
